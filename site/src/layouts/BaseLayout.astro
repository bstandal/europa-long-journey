---
import "@fontsource/bodoni-moda/400.css";
import "@fontsource/bodoni-moda/500.css";
import "@fontsource/manrope/400.css";
import "@fontsource/manrope/500.css";
import "@fontsource/manrope/600.css";
import "../styles/global.css";
import IconSprite from "../components/IconSprite.astro";

type Props = {
  title: string;
  description: string;
  bodyClass?: string;
};

const { title, description, bodyClass = "" } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
const socialImage = new URL(
  `${import.meta.env.BASE_URL}assets/social-europa.jpg`,
  Astro.site,
);
const analyticsWebsiteId = import.meta.env.PUBLIC_UMAMI_WEBSITE_ID?.trim();
const analyticsScriptUrl =
  import.meta.env.PUBLIC_UMAMI_SCRIPT_URL?.trim() ||
  "https://cloud.umami.is/script.js";
const analyticsDomains =
  import.meta.env.PUBLIC_UMAMI_DOMAINS?.trim() || canonical.hostname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#111b22" />
    <meta name="description" content={description} />
    <link
      rel="icon"
      type="image/svg+xml"
      sizes="any"
      href={`${import.meta.env.BASE_URL}favicon.svg`}
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href={`${import.meta.env.BASE_URL}apple-touch-icon.png`}
    />
    <link rel="canonical" href={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="EUROPA" />
    <meta property="og:locale" content="en_GB" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:secure_url" content={socialImage} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta
      property="og:image:alt"
      content="A luminous route crossing a landscape of European history"
    />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImage} />
    <meta
      name="twitter:image:alt"
      content="A luminous route crossing a landscape of European history"
    />
    {
      analyticsWebsiteId && (
        <script
          is:inline
          defer
          src={analyticsScriptUrl}
          data-europa-analytics
          data-website-id={analyticsWebsiteId}
          data-domains={analyticsDomains}
          data-auto-track="false"
          data-exclude-search="true"
          data-exclude-hash="true"
          data-do-not-track="true"
          data-performance="true"
        />
      )
    }
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body class={bodyClass}>
    <IconSprite />
    <a class="skip-link" href="#main-content">Skip to main content</a>
    <slot />
    <script>
      import "../scripts/analytics";
    </script>
  </body>
</html>
