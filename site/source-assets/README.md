# Source artwork

These lossless PNG files are retained as editable source material. The site serves the optimized WebP
versions from `public/assets/`, so keeping the PNG originals outside `public/` prevents 4.6 MB of unused
files from being copied into every production build.
