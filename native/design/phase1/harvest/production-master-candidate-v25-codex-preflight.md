# Harvest candidate v25 — Codex preflight

Status: `CODEX_PROVISIONAL_NON_SHIPPING_REVIEW`

Candidate:
`production-master-candidate-v25-grain-local-composite.png`
(`5ddcef3511e0b29dd0ace635a26135290bcf50402531b7d55b60e30422873762`)

The complete frame and the hearth, people, storage and grain regions were
inspected at decoded pixel resolution on 24 July 2026. This review grants no
editor, production-master, layer-DAG or shipping authority.

## Passed provisional checks

- The canvas is exactly `1290 × 2796`; no upscaling or resampling follows the
  v23 full-resolution composite.
- Camera, portrait composition, canopy, settlement, fields, livestock,
  storage and every person outside the two authorised corrections remain the
  selected v11 pixels.
- The hearth vessel reads as a thick, irregular, handleless, soot-darkened
  hand-built fired-clay pot. It sits against the existing stones and flame and
  has no bail, chain, suspension or metallic highlight.
- The finite foreground mound reads as thousands of small hulled cereal
  kernels with scattered chaff. Kernel scale is materially smaller than the
  earlier bean-like v24 pieces.
- The hearth and grain edits show no visible rectangular edge or colour seam
  at one-to-one inspection. Raw-frame hashes prove that every pixel outside
  each recorded non-zero mask rectangle is unchanged.
- The near-black frame separates the scene while readable midtones retain
  faces, timber, clay, wicker, grain and work. The harvest remains the clearest
  large material mechanism.
- No obvious extra limb, missing hand, duplicated face, modern object, text,
  symbol or interface element was found in the inspected raster.

## Gates still open

- The three stores are visually distinct objects, but empty, receiving and
  committed states do not exist. Their causal legibility can be judged only
  after the authored state layers run in the interaction.
- Parallax disocclusion, hair and hand edges, rain and smoke occlusion, clean
  plates, depth/light masks, state-change masks and the two Reduce Motion
  plates have not been produced.
- The contract forbids the 86-file production DAG until one candidate receives
  the artistic production-master decision. Candidate v25 therefore remains a
  flattened, non-shipping input to that later gate.
- Simulator motion, integrated responsive audio, VoiceOver parity and hard-kill
  restoration must be judged in the assembled development scene.
- Physical display, thermal and interruption checks remain deferred to the
  consolidated device gate.
